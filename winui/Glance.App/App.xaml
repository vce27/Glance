<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="Glance.App.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Glance.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Original Glance / Fluent accent (#0078D4), not OS accent #0067C0 -->
            <Color x:Key="SystemAccentColor">#FF0078D4</Color>
            <Color x:Key="SystemAccentColorLight1">#FF429CE3</Color>
            <Color x:Key="SystemAccentColorLight2">#FF60ABE4</Color>
            <Color x:Key="SystemAccentColorLight3">#FF88C1EC</Color>
            <Color x:Key="SystemAccentColorDark1">#FF005A9E</Color>
            <Color x:Key="SystemAccentColorDark2">#FF004578</Color>
            <Color x:Key="SystemAccentColorDark3">#FF003354</Color>

            <SolidColorBrush x:Key="SystemAccentBrush" Color="{StaticResource SystemAccentColor}" />
            <SolidColorBrush x:Key="AccentFillColorDefaultBrush" Color="{StaticResource SystemAccentColor}" />
            <SolidColorBrush x:Key="AccentFillColorSecondaryBrush" Color="{StaticResource SystemAccentColorDark1}" />
            <SolidColorBrush x:Key="AccentFillColorTertiaryBrush" Color="{StaticResource SystemAccentColorLight1}" />
            <SolidColorBrush x:Key="AccentSoftBrush" Color="#1F0078D4" />

            <Style x:Key="GlancePrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="Background" Value="{StaticResource SystemAccentBrush}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderBrush" Value="{StaticResource SystemAccentBrush}" />
                <Setter Property="Padding" Value="12,6" />
            </Style>

            <Style x:Key="GlanceGhostButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="Padding" Value="8,6" />
            </Style>

            <Style x:Key="GlancePinToggleStyle" TargetType="ToggleButton" BasedOn="{StaticResource DefaultToggleButtonStyle}">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="Padding" Value="8,6" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
